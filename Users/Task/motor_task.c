#include "main.h"
#include "cmsis_os.h"
#include "motor.h"

void StartmotorTask(void const * argument)
{
   /* uint32_t time = xTaskGetTickCount();
	
		Motor motor;
		Motor_Init(&motor, &htim8, TIM_CHANNEL_2, &htim3, BIN1_GPIO_Port, BIN1_Pin, BIN2_GPIO_Port, BIN2_Pin);
    while(1)
    {
        motor.Run(&motor, 7200-1);
        osDelayUntil(&time, 1);
        //osDelay(500);
    }*/

}